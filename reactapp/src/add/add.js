import * as React from 'react';

const Add = () => {
    const [value, setValue] = React.useState(0);
    
    return (
        <div>
            Add Posts!
        </div>
    );
}

export default Add;
